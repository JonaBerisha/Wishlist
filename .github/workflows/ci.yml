name: CI
on:
  pull_request:
    branches:
      - main

  jobs:
    test:
      runs-on: ubuntu-latest

      steps:
        - uses: actions/checkout@4

        - uses: actions/setup-java@4
          with:
            java-version: '21'
            distribution: 'temurin'

        - run: mvn clean test